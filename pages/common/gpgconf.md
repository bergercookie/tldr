# gpgconf

> Query and modify the .gnupg configuration contents.

- Reload the gpg agent configuration:

`gpgconf --reload gpg-agent`

- List all the options of the currently running GPG agent

`gpgconf --list-options gpg-agent`
